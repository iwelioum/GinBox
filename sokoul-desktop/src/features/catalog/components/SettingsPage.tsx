// SettingsPage.tsx — Paramètres de l'application
// Squelette Phase 1 — implémentation Phase suivante

export default function SettingsPage() {
  return (
    <div style={{ padding: '4rem 2rem', color: 'var(--color-text-primary)' }}>
      <h1 style={{ fontSize: '2rem', fontWeight: 700, marginBottom: '1rem' }}>Paramètres</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>Paramètres de l'application — à implémenter</p>
    </div>
  );
}

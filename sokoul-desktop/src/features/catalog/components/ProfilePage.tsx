// ProfilePage.tsx — User profile management
// Skeleton Phase 1 — implementation in next phase

export default function ProfilePage() {
  return (
    <div style={{ padding: '4rem 2rem', color: 'var(--color-text-primary)' }}>
      <h1 style={{ fontSize: '2rem', fontWeight: 700, marginBottom: '1rem' }}>Profiles</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>Profile management — to be implemented</p>
    </div>
  );
}
